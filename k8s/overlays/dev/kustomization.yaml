apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../base/backend

labels:
  - pairs:
      environment: dev

configMapGenerator:
  - name: backend-config
    literals:
      - LOG_LEVEL=debug
      - API_TIMEOUT=30000
      - NODE_ENV=development