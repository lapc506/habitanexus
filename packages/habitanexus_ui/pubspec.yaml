name: habitanexus_ui
description: "Shared HabitaNexus UI package — Atomic Design widgets y tema M3."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
